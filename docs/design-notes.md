# 最初の試作

完成デザインではなく、ゆうちゃんと見ながら育てるための第一案です。

## 画面

- ブランド名「ハルノヒト」を最も大きく表示。
- 「ちいさなデジタル工房」は小さな肩書き。
- 提供された線画の色になじむ、温かい紙色の背景。
- PCは左に文字、右に女の子。スマートフォンは上に文字、下に女の子。
- 原本の透明な余白を含む16:9の共通ステージ。女の子も花びらも同じステージ全体に配置するため、元画像の解像度差で位置がずれません。
- スマートフォンは共通ステージ自体を拡大・配置して余白をトリミングします。女の子の左右端は画面内に残し、元画像の下端は加工しません。

## 動き

名前は0.52秒の時間差で、一文字につき4.2秒かけて登場します。
最初の待ち時間を含め、約6.9秒で全体が定位置に収まります。
文字サイズは最初の試作より約1割大きくしました。肩書きは維持しています。
初回の約7秒の登場を、同じCSSアニメーション `name-arrives` の再起動で繰り返します。21秒周期なので、最後の文字が収まってから約14秒静止します。
各文字の4.2秒、0.52秒差、最初の0.6秒、キーフレームと緩急は初回と同じです。再生のたびに初回と同じように一度文字が消えて現れます。
画面上の停止ボタンは置かず、OSの「動きを減らす」設定ではすべてのアニメーションと目ぱちを止めます。

桜は最初の1枚から始まり、その後は約1.4秒おきに1枚ずつ追加します。
各出現には2.8秒かけます。PCは48枚（約70秒で出揃う）、スマートフォンは30枚（約45秒）。
7秒時点で4枚、24秒時点で16枚、75秒時点で48枚が見える、A→B→Cの流れです。
PCの移動の片道周期は約10.7・12.9・15秒（前回から速度12％増）。回転の周期も同じ比率で短縮しています。
スマートフォンでは広い往復移動を使い、画面上で桜の動きが分かるようにしています。
既存10素材を再利用し、大きさ・角度・透明度・位置・周期を変えています。
元のPNGは加工せず、透明な余白を含む共通座標系を保っています。

## 顔の禁止エリア

1920×1080の共通座標で、左755・上390・右1145・下800の長方形を保護します。
目・頬・口・あごを含む顔全体に余白を加えています。
各花びらの表示部分を囲む円を計算し、サイズ・全回転・移動幅を含む範囲がこの長方形に触れる配置は生成しません。
加えて、桜の親レイヤーに同じ範囲をくり抜く固定のクリッピングを設定しています。
女の子の画像にはクリッピングをかけません。画面幅が変わっても禁止エリアは女の子と同じ比率で拡大縮小します。

## 次に調整する候補

`dist/scripts/main.js` の `positions` が出現順と中心位置、`faceExclusion` が顔の禁止エリアです。
`sourceBounds` は原本PNG内の見える部分の範囲です。
出現間隔・移動周期・大きさの変更も同じファイルで調整できます。
スマートフォンは既存22枚と追加8枚の計30枚を表示します。モバイル専用の連番で出現時刻を設定するため、非表示の花びらの分だけ間隔が空くことはありません。

## 2026-09-18 調整の確認

PC 1440×900、スマートフォン390×844・320×568、タブレット768×1024を確認。
横にはみ出すスクロールなし、全画像読み込み成功、顔の禁止エリアに触れる移動範囲は0。
停止・再開、動きを減らす設定（アニメーション0件、文字を表示）を確認。

## 今回の範囲外

トップページの続き、プロフィール、お問い合わせ、サービス、作品一覧、左下の2人の上下移動や追加演出、一般公開と独自ドメイン。

## 第3調整

PCは34→48枚（約1.41倍）、スマートフォンは22→30枚（約1.36倍）。
追加分は画面内に残りやすい位置へ分散し、顔の禁止エリアと移動範囲検証は維持しています。
レイアウトと原本画像は変更していません。LAN確認サーバーはMacのプライベートWi-Fiアドレスだけに追加でバインドし、既存のlocalhost表示も維持します。
実際のiPhoneでの表示・通信は、同じWi-Fiに接続した実機で確認します。

## 第4調整

PCの桜の移動と回転を前回より12％速くしました。枚数と出現間隔は変更していません。
スマートフォンは桜の横の移動幅を共通ステージ上で85〜137px、縦を50〜89pxに広げました。390px幅の画面では約27〜43pxと約16〜28pxに相当します。
各桜は顔から離れる方向へ動き、PC用とスマートフォン用の両方の移動範囲で顔の禁止エリアを検査します。固定のクリッピングも維持します。
スマートフォンの女の子を共通ステージごと画面幅の3％だけ右へ移しました。桜と顔の位置関係は変わりません。
初回のブランド名の登場はそのままです。次の動きは初回終了から27秒後に始まり、5秒かけて元に戻ります。これを32秒周期で繰り返します。
PC 1440×900、スマートフォン390×844・320×568で、画像48枚の読み込み、横スクロールなし、顔との重なり0、繰り返しと動きを減らす設定を確認しました。

## 第5調整

第4調整の横に広がる繰り返しを、初回登場を短く思い出すような一文字ずつの動きに変更しました。初回の登場、桜、女の子、画面配置はそのままです。
各文字は約26秒静止したあと、少しだけ淡く・下に動いて約4秒で戻ります。0.18秒ずつ順に動き、32秒周期で繰り返します。完全には消えず、文字間隔も広がりません。

## 第6調整

第5調整の独立した `name-echo` を削除し、既存の `name-arrives` をそのまま再起動する方式へ変更しました。新しいキーフレームは追加していません。
34秒周期で初回と同じ約6.9秒の登場を繰り返し、その間は約27秒静止します。停止ボタンでは再起動のタイマーも一時停止し、動きを減らす設定では再起動を行いません。

## 第7調整

再生間隔だけを34秒から21秒へ短縮しました。約6.9秒の登場のあと、約14.1秒静止します。キーフレーム、各文字の時間差、速度とほかの画面要素は変更していません。

## 第8調整：左下の小さな仲間

`.corner-companions` を女の子・桜の `.scene` と別のまとまりとして追加しました。
PCの画像枠はゆうちゃん90px、Haru74px。左から画面幅の4％（20〜80px）、下から30.4pxに配置し、Haruを6px高くしています。
スマホは通常64px・54pxの画像枠で、左から8px、下から16px。389px以下では60px・50px、360px以下では54px・44pxにし、2人を寄せて停止ボタンの左側に収めます。
画像枠には原本の透明な余白も含まれます。原本4枚は加工せず保存し、Web用にも同じPNGをコピーしています。

閉じ目素材には体がないため、基本画像は表示し続け、同じ1254×1254の座標枠で目の部分だけを上に表示します。眉・口・輪郭・体は基本画像のままです。
画像は低い読み込み優先度にし、両状態の読み込みと表示準備ができてから目ぱちを開始します。
最初は約8.5〜11.5秒待ち、その後は閉じ目から戻って6.5〜10.5秒後に、どちらか1人だけが0.22秒閉じます。
同じキャラクターが9.5秒以内に続けて閉じないようにし、間隔が長いときだけ同じキャラクターが続くこともあります。
停止ボタン、動きを減らす設定、タブを離れた場合はタイマーを止め、開き目に戻します。桜と文字の動きの処理は変更していません。

PC 1440×900、スマホ390×844・375×667・320×568、768×1024、700×640で表示を確認。停止ボタン・ブランド名・中央の顔との重なりなし、開閉の表示枠のずれなし、同時の目ぱちなしを確認しました。
高さ640px未満では、従来の最小高さ設定により下端までスクロールして表示します。


## 第9調整：PCの仲間とスマホの余白

手動停止ボタンのHTML・専用CSS・クリック処理・一時停止状態の管理を削除しました。
OSの `prefers-reduced-motion` と、その変更時の停止・再開は維持しています。
ブランド名は既存の初回キーフレームと時間差を使い、21秒周期のままです。桜の生成・移動・顔の禁止エリアには変更を加えていません。

PCの画像枠をゆうちゃん162px、Haru133.2pxへ、どちらも1.8倍にしました。
「ノ・ヒ」の下の余白へ移動し、1440px幅ではまとまりの左端が約281px、下端は従来どおり画面下から30.4pxです。
Haruは10.8px高くして、従来の2人の組み合わせを維持します。1100px以下のPC表示では女の子との重なりを避けるため、横位置を少し左へ調整します。
700px以下では2人を非表示にし、目ぱちのタイマーも休止します。素材とPC用の目ぱちは保持しています。

1920×1080、1440×900、1024×768、900×640、768×1024、700×640、390×844、375×667、320×568を確認しました。
変更前後で、女の子・肩書き・ブランド名・ステージの表示位置と寸法が一致することを確認しています。
全幅で停止ボタンなし、横スクロールなし。PCの目ぱち・21秒の文字再生、スマホでの目ぱち休止、OS設定による全アニメーション停止と再開を確認しました。
iPhone実機は遠隔操作していません。同じWi-Fi用URLへのサーバー応答をMacから確認しました。

## 第10調整：PCの2人の足元と目ぱち

PC左下の2人を下へ16px移動し、表示枠を画面下から14.4pxにしました。
ゆうちゃんは162pxのまま、Haruは133.2pxから120px（約90％）へ縮小しました。
Haruの画像下側には透明な余白があるため、Haruだけ表示枠をさらに4px下げ、見える足元をゆうちゃんとそろえています。
横位置、女の子、桜、ブランド名、肩書き、スマートフォン用の配置は変更していません。

PCの目ぱちは、最初の待ち時間を8.5〜11.5秒から4.25〜5.75秒へ、その後の待ち時間を6.5〜10.5秒から3.25〜5.25秒へ短縮しました。
片方だけを0.22秒閉じる仕組みと、連続して同じ人を選ばないための時間差はそのまま残し、判定時間も9.5秒から4.75秒へ半分にしています。
700px以下では2人を隠して目ぱちを止め、OSの「動きを減らす」設定でも停止します。

PC 1920×1080、1440×900、1024×768、900×640、768×1024、スマートフォン390×844・375×667・320×568を確認。
女の子と文字の位置は前回と同じ、横スクロールなし、PCの足元の見え方・まばたきの時間差・21秒の文字再生、スマートフォンでの非表示、OS設定での停止を確認しました。

## 第11調整：PCの2人を少し左下へ

PCの2人のまとまりを1440px幅で約84px左、8px下へ移動しました。小さめのPC画面でも左側に収まるよう、既存の幅別の位置調整を保っています。
ゆうちゃん162px、Haru120px、Haruの足元を合わせる4pxの補正、目ぱちの頻度と時間差は変更していません。
PNGは加工せず、まとまりのCSSに `saturate(.9) contrast(.96)` をかけ、透過させずに色だけ少しなじませています。

1440×900では2人の見える足元は画面下から約6px、中央の女の子のスカート端は画面下から約52pxです。女の子の位置を維持して2人を下へ移動する指定を優先したため、スカート端より2人の足元が約46px下になります。
PC 1920×1080、1440×900、1024×768、900×640、768×1024とスマートフォン390×844・375×667・320×568で、ブランド名・中央の女の子・桜の配置が変わらず、2人の足元の見え方・目ぱち・スマートフォンでの非表示・動きを減らす設定を確認しました。
